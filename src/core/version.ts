/**
 * src/core/ holds all game rules as pure, engine-free TypeScript
 * (zero Phaser imports) so Vitest can run them in Node. See
 * docs/architecture.md §2.
 */
export const CORE_VERSION = '0.0.0';
